# Server URL - defaults to window.location.origin in development mode
# This allows the web UI to automatically work when accessed via network IP
# Uncomment and set this to override for specific use cases
# VITE_DETERMINANT_SERVER_URL=http://localhost:10110
